@model CandyShop.Domain.Models.Candy
@using System.Text.Encodings.Web

<h1>@HtmlEncoder.Default.Encode(Model.Name)</h1>

<div class="thumbnail">
    <img src="@HtmlEncoder.Default.Encode(Model.ImageUrl)" alt="@HtmlEncoder.Default.Encode(Model.Name)" />
    <div class="caption-full">
        <h3 class="pull-right">@HtmlEncoder.Default.Encode(Model.Price.ToString("C"))</h3>
        <h3><a href="#">@HtmlEncoder.Default.Encode(Model.Name)</a></h3>
        <p>@HtmlEncoder.Default.Encode(Model.Description)</p>
    </div>
    <div class="addToCart">
        <p class="button">
            <a class="btn btn-primary" asp-controller="Shoppingcart" asp-action="AddToShoppingCart"
               asp-route-candyId="@Model.CandyId">Add to Cart</a>
        </p>
    </div>
</div>
